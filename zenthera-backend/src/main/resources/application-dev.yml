spring:

  datasource:
    url: jdbc:postgresql://localhost:5432/zenthera_db
    username: postgres
    password: 220122
    driver-class-name: org.postgresql.Driver

  flyway:
    baseline-on-migrate: true
  jpa:
    hibernate:
      ddl-auto: validate

    show-sql: true

    properties:
      hibernate:
        format_sql: true

jwt:
  secret: "ZentheraSuperSecretKey2026JwtSecuritySpringBoot123456"
  expiration-ms: 86400000

server:
  port: 8080